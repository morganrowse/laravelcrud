@extends('layouts.app')

@section('title', 'Create new %model_singular%')

@section('content')
    <h1>Create new %model_singular%</h1>

    <form method="POST" action="{{route('%model_store_route%')}}">
        {{csrf_field()}}

        %model_form_fields%

        <div class="form-group">
            <button type="submit" class="btn btn-sm btn-primary">Submit</button>
        </div>
    </form>
@endsection
